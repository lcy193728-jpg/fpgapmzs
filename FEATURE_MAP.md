# 功能实现对照表（赛题要求 ↔ 当前代码）

- 工程：`E:\FPGA\ALST\fpgapmzs`（安路 TD EDA 6.2 / EG4S20BG256，板卡 HX4S20C）
- 赛题：2026 嵌赛 FPGA·安路赛道 **选题一《基于安路 EG4S20 的 HDMI 多媒体播放系统》**
- 版本：commit `e888998`（2026-09-16），比特流已固化板载 SPI Flash，上电即运行

---

## 一、基础要求（必须全部实现）

| # | 赛题要求 | 状态 | 实现说明 / 主要代码 |
| - | -------- | ---- | ------------------- |
| 1 | TF 卡 BMP 读取与 HDMI 显示 | **已实现**（上板验证） | SPI 只读 TF 卡（初始化后 25MHz），不解析 FAT，按 8 扇区步进扫 BMP 头并严格校验 `"BM" + 640×480×24bit + file_len`；像素写入片内 SDRAM 帧缓存（单帧 307200 × 32bit，RGB888 打包），HDMI 以 640×480@60 实时输出。<br>`src/sd_card_bmp.v`、`src/bmp_read_auto.v`、`src/frame_read_write.v`、`src/hdmi/` |
| 2 | 图片切换与轮播（按键手动 + 自动） | **已实现**（上板验证） | 自动轮播 3s/张；手动切图：`KEY1` 切到「图片模式」后 `KEY3`=下一张 / `KEY2`=上一张（首张按 `KEY2` 回自动轮播）。另有按拨码切场景时的分区重载切换。<br>`src/ui_key_ctrl.v`、`src/bmp_read_auto.v` |
| 3 | HDMI 音频输出（48kHz 测试音） | **未实现** | 当前 `hdmi_tx` 例化仅接视频端口（`VGA_HS/VS/DE/RGB` + 4 路 TMDS），**未接内嵌音频（数据岛 ADATA/ACLK/ALRC）**，板上无声音输出。下一步须按官方 APUG092《HDMI 1.4b Transmitter》补齐音频端口并在消隐期嵌入音频包 |
| 4 | 系统稳定运行（固化、上电即运行） | **已实现**（上板验证） | 比特流 `program_spi + verify_spi` 已写入板载配置 Flash（`download_flash.tcl`），无需 PC 下载；顶层用 POR 计数器产生约 10.5ms 上电复位，保证 PLL/SDRAM/HDMI 就绪后再放行 |

---

## 二、扩展要求（加分项）

| # | 扩展项 | 状态 | 实现说明 |
| - | ------ | ---- | -------- |
| 1 | 图层叠加与字幕效果 | **已实现** | 自研汉字 OSD 引擎（16×16 点阵字库，片内 BRAM 生成），4 级叠加流水：`背景帧 → 菜单层 → 迎新区信息层 → 会议/抢答/应急场景层 → 末级调节层`。<br>· 滚动字幕 5 处：菜单底部宣传语、迎新底部报到流程、会议会务提示、抢答须知、应急疏散告警（每帧左移 1px、无缝循环）<br>· Alpha 混合：欢迎语金带 50%、报到/联系卡 75% 衬底；菜单层为不透明覆盖<br>· 时间戳/时长：会议场景实时显示「已运行 HH:MM:SS」<br>`src/osd_engine.v`、`osd_menu.v`、`osd_welcome.v`、`osd_scene.v` |
| 2 | 图片切换与转场特效 | **部分实现** | 已做：场景/菜单切换的**整帧淡入淡出**（alpha 逐帧降黑 → 等底层新图写完 → 逐帧淡入；应急期间强制不淡出）。<br>未做：滑动/推移转场、双帧缓存（当前 SDRAM 只用单帧 `read_addr_0`） |
| 3 | 图片缩放适配 | **已实现**（超出要求） | 自研可分离双线性插值缩放引擎，8 档 **25/33/50/67/100/150/200/300%**（Q16 坐标映射 + Q8 权重 + 8bit 饱和钳位），串在 TF→SDRAM 写通路；**100% 档为真 1:1 旁路**（与原图逐像素一致）；缩小图水平居中/顶部对齐，放大按中心裁剪；插值拆 4 级流水保证 100MHz 域时序收敛。<br>`src/bmp_scale.v` |
| 4 | 实时调节与信息叠加 | **已实现** | · 16 档亮度（增益 ×0.5~×1.44，`KEY1` 切「亮度模式」+ `KEY2/KEY3` 调节）<br>· 8 档缩放档位实时切换（即时重读当前图生效）<br>· 屏上 OSD 提示 HUD：亮度条(16 档) / 缩放条(8 档) / 轮播-手动状态卡(绿▶自动 / 橙▮▮手动)，变化即弹出、30 帧后自动消失<br>· 8 位数码管：场景号 / 当前参数值 / 功能模式号 / 分隔线 / A(自动)-H(手动)<br>`src/display_adjust.v`、`src/ui_key_ctrl.v`、`src/seg_scan.v`、`src/top.v` |
| 5 | 音频可视化（FFT/频谱/波形） | **未实现** | 依赖基础 3 的 HDMI 音频通路，须先打通 I2S 采集与音频嵌入，再取 PCM 做 DFT/FFT 渲染频谱柱 |

---

## 三、赛题之外的自研功能（实用性与创新点）

| 功能 | 说明 |
| ---- | ---- |
| **四场景智能终端** | 上电进「首页菜单」（整帧 OSD 矢量自绘，零素材依赖）→ 拨码选场景：迎新展示 / 会议信息公示 / 学科竞赛抢答 / 应急告警 |
| **一卡四区（免烧录换素材）** | TF 卡按扇区划分 4 个独立素材区（`Z_WEL/Z_MEET/Z_QUIZ/Z_ALARM`），各场景只播本区图片；更新素材只需换卡或重拷图片，不用重新综合烧录 |
| **多路硬件并行仲裁（抢答）** | 4 路外扩抢答台按键统一时钟域同步采样 + 片内并行仲裁（先到先得、锁存后忽略后续、同拍多路按 1>2>3>4），10s BCD 倒计时与超时判定；外扩引脚已约束（`quiz_btn[3:0]/quiz_start/quiz_reset`） |
| **场景仲裁：应急最高 + 先触发先锁定** | `SW4`=应急最高优先级电平直通（拨上即切、拨回即解）；`SW1~SW3` 普通场景以**先被拨上的为准并锁定**，持锁场景被拨回时让给仍开着的优先者 |
| **统一按键交互（蓝桥风格）** | `KEY1` 循环功能模式 `0图片/切图 → 1亮度 → 2分辨率`，`KEY2/KEY3` 永远调「当前模式参数」；模式寄存器做总闸，三条门控互斥不串；`KEY4` 已释放 |
| **按键消抖重写** | 统一为「以消抖后电平为唯一基准 + 连续 10ms 偏离才采纳」，长按/连按/抖动都不丢键、不重复 |
| **工程健壮性** | 上电自动复位 POR、场景切换黑场等待 `img_busy` 释放（45 帧超时兜底）、写通路节流防 FIFO 溢出、`bmp_scale` 关键路径流水化 |
| **开发工具链** | `tools/gen_osd_font.py`（字库与几何表生成，改文案一处改）、`tools/find_bmp.py`（卡内 BMP 扇区定位→生成分区表）、`download_flash.tcl`（一键固化）、10 套 ModelSim 回归用例 |

---

## 四、关键参数速查

| 项目 | 数值 |
| ---- | ---- |
| 输出分辨率 / 刷新 | 640×480@60Hz（像素时钟 25MHz） |
| 时钟 | `sys_pll` 50M→100M(卡/控制域)、125M(SDRAM)、125M 移相；`video_pll` 50M→25M(像素)、125M(TMDS 5×) |
| TF 卡读速 | 初始化低速 → 成功后 25MHz；喂数约 96 拍/像素（远大于处理需求） |
| 轮播间隔 | 3s/张 |
| 缩放档位 | 0~7 = 25%/33%/50%/67%/100%(默认)/150%/200%/300% |
| 亮度档位 | 0~15（默认 8 = ×1.0） |
| 抢答倒计时 | 10s（BCD 显示） |
| 场景分区 | 迎新区 126656~130272(2 张)、会议区 130272~133888(2 张)、抢答区 133888~135696(1 张)、应急区 135696~135704(1 张) |
| 资源占用 | LUT 3725 / 19600、FF 3417 / 19600、BRAM 58、DSP 28 |
| 时序 | Setup WNS −5291ps（同量级优于官方例程 −6565ps）、Hold WNS +471ps、FEPS 94 / 0 |

---

## 五、现场演示操作路径

1. 上电 → 屏上出现全屏菜单（数码管第 1 位 = 0）
2. 拨 `SW1` → 迎新场景轮播 + 顶部欢迎语/报到信息/滚动流程字幕
3. 按 `KEY1` 到第 5 位数码管 = `0`（图片模式）→ 按 `KEY3` 切换下一张（第 8 位变 `H`）、按 `KEY2` 回上一张
4. 按 `KEY1` 到模式 `1` → `KEY3/KEY2` 调亮度，屏上弹亮度条
5. 按 `KEY1` 到模式 `2` → `KEY3/KEY2` 调分辨率档，屏上弹缩放条，画面按新档位重画
6. 拨 `SW2` → 会议公示（公告翻页 + 已运行时长 + 滚动会务提示）
7. 拨 `SW3` → 抢答（接外扩抢答台或用杜邦线短接外扩口对应引脚，验证「先按先得 + 锁存 + 倒计时」）
8. 拨 `SW4` → 应急最高优先级（顶部红条闪烁 + 警示图标 + 滚动疏散告警），拨回即解除

---

## 六、待办与风险

| 项 | 说明 |
| -- | ---- |
| **基础 3 HDMI 音频（最高优先级）** | 必做项，缺失会直接影响基础分。需查 APUG092 发送器音频端口，在消隐期嵌入音频数据岛 |
| 扩展 5 音频可视化 | 排在基础 3 之后 |
| 抢答外设实物 | 引脚已约束（外扩口 `GPIOA0~A5`），但抢答台/控制盒实物接线位置需按板卡丝印核对；无实物时可用杜邦线短接对应引脚演示 |
| 一卡四区素材维护 | 换卡或重新拷贝图片后，扇区值会变，必须重跑 `python tools/find_bmp.py --drive F --sizes 2,2,1,1 --names WEL,MEET,QUIZ,ALARM` 并同步 `top.v` 的 `Z_*` 常量 |
| 转场特效 | 如需更高扩展分，可补「滑动转场」（需双帧缓存或行缓存滑动） |

---

## 七、代码模块索引

| 模块 | 作用 |
| ---- | ---- |
| `src/top.v` | 顶层：时钟/PLL、场景分区表、全部模块例化与视频链路拼接 |
| `src/sd_card_bmp.v` / `bmp_read_auto.v` | TF 卡 SPI 读、BMP 头扫描、分区轮播/手动切图状态机 |
| `src/bmp_scale.v` | 双线性插值缩放引擎（8 档，100% 旁路，4 级流水） |
| `src/frame_read_write.v` / `frame_fifo_write.v` / `frame_fifo_read.v` | 异步 FIFO 桥接 + SDRAM 帧读写（含 180° 翻转修正） |
| `src/scene_control.v` | 拨码场景选择、应急最高 + 先触发先锁定仲裁、运行时长 |
| `src/quiz_ctrl.v` | 4 路抢答并行仲裁 + 倒计时 |
| `src/osd_engine.v` | 像素坐标重建 + 通用叠加底座 |
| `src/osd_font_rom.v` / `tools/gen_osd_font.py` | 汉字点阵字库（BRAM）与生成脚本 |
| `src/osd_menu.v` | 首页菜单层（全屏矢量）+ 应急红条 |
| `src/osd_welcome.v` | 迎新信息层（欢迎语/报到/联系/滚动字幕，分区 Alpha） |
| `src/osd_scene.v` | 会议 / 抢答 / 应急三场景层 |
| `src/display_adjust.v` | 16 档亮度、淡入淡出、操作提示 HUD |
| `src/ui_key_ctrl.v` | 按键消抖 + 功能模式/参数统一交互 |
| `src/seg_scan.v` | 8 位数码管扫描显示 |
| `src/hdmi/` | HDMI 发送器（官方加密 IP，TMDS 编码 + 并串） |
| `tb/` `sim/` | 10 套 ModelSim 测试平台与运行脚本 |
| `BOARD_CHECKLIST.md` | 上板前自检清单（综合列表/黑盒、下载分诊、RTL 易错、回归纪律） |
