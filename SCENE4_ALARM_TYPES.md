# 场景四：四类应急告警

本版本基于 `dev_sim` 的 HDMI 音频工程增加四类告警显示。原有防空警报 DDS 在整个应急期间持续播放，切换告警类型不会重新触发或中断音频，也不读取 TF 卡。

## 上板操作

1. 将 `SW4` 拨到有效位置进入应急场景。进入时默认显示 **火灾警报（00）**，防空警报立即开始。
2. 每短按一次 `KEY4 (C1)`，告警类型依次循环：
   - `00` 火灾警报
   - `01` 地震避险
   - `10` 恶劣天气
   - `11` 临时疏散
   - 再按一次回到 `00`
3. 将 `SW4` 解除后退出应急；再次进入时重新从火灾警报开始，持续时间归零。

`KEY1/KEY2/KEY3` 的原有图片、亮度、缩放和轮播周期功能保持不变。四类页面均显示告警级别、地点、行动指令、禁止/注意事项、持续时间、“告警尚未解除”和管理员解除提示。图标及背景由 FPGA 实时绘制，无需向 TF 卡增加图片。

## 修改文件

- `src/emergency_alarm_ctrl.v`：KEY4 消抖、四类选择、告警持续时间。
- `src/emergency_multi_overlay.v`：四类告警信息页、矢量图标和字幕。
- `src/emergency_font_rom.v`：应急页面中文字形 ROM。
- `audio_final/integrated/top_final.v`：连接控制、画面和既有防空警报音频链。
- `tb/tb_emergency_alarm_ctrl.v`：四类循环和计时自检。

字库由 `tools/gen_emergency_osd.py` 生成；修改应急文案后需重新运行该脚本。
